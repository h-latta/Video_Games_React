import SearchBar from "../SearchBar/SearchBar";
const NavBar = (props) => {
    return ( 
        <div>
            <h2>Hello world!</h2>
            <SearchBar />
        </div>
     );
}
 
export default NavBar;